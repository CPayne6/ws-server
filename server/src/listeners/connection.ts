import { Listener } from './listeners.types'
import { WsEvent } from '../events/events.types'

export const connection: Listener = (ws, req) => {
  // Look for JWT

  // Create JWT

  
  const 
  ws.emit(WsEvent.INIT, )
}